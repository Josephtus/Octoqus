# =============================================================================
# requirements.txt - Backend Python Bağımlılıkları
# Expense Tracking & Social Finance Platform
# Tüm versiyonlar sabitlenmiştir (production güvenliği)
# =============================================================================

# --- Web Framework ---
sanic==23.12.1                  # Async HTTP + WebSocket framework
sanic-ext==23.12.0              # Dependency injection, OpenAPI desteği

# --- ORM & Database ---
sqlalchemy[asyncio]==2.0.30     # Async ORM (asyncio engine)
aiomysql==0.2.0                 # SQLAlchemy async MySQL driver
alembic==1.13.1                 # Veritabanı migration yönetimi

# --- Redis ---
redis[hiredis]==5.0.4           # Async Redis client (cache, pub/sub, rate limit)

# --- Auth & Security ---
pyjwt==2.8.0                    # JSON Web Token
bcrypt==4.1.3                   # Şifre hashing
cryptography==42.0.7            # JWT RSA desteği (ileride opsiyonel)

# --- Validation & Serialization ---
pydantic==2.7.1                 # Data validation & schema
pydantic-settings==2.3.0        # .env okuma (Settings sınıfı için)

# --- Object Storage (S3-compatible) ---
aiobotocore==2.13.0             # Async S3 client (MinIO / AWS S3)
aiofiles==23.2.1                # Async dosya okuma (upload pipeline)

# --- Task Queue & Background Jobs ---
arq==0.25.0                     # Redis tabanlı async task queue (Cash Flow algo)

# --- Utilities ---
python-dotenv==1.0.1            # .env yükleme (geliştirme ortamı)
ujson==5.9.0                    # Ultra-fast JSON serialization
python-multipart==0.0.9         # Multipart form / file upload desteği
httpx==0.27.0                   # Async HTTP client (dış servis istekleri)
aiosmtplib==3.0.1               # Async SMTP client (Şifre sıfırlama vb.)
email-validator==2.1.1          # Pydantic EmailStr desteği için

# --- Logging ---
structlog==24.1.0               # Yapılandırılmış loglama (JSON output)

# --- Testing (CI/CD) ---
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-sanic==1.9.1
httpx==0.27.0                   # Test HTTP client
factory-boy==3.3.0              # Test fixture factory
